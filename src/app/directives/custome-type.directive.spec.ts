import { CustomeTypeDirective } from './custome-type.directive';

describe('CustomeTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomeTypeDirective();
    expect(directive).toBeTruthy();
  });
});
